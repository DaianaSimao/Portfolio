<body>
    <ul class="nav justify-content-end">
        <li class="nav-item">
          <%= link_to "Português", new_coin_path(locale: "pt-BR"), class:"nav-link"  %>
        </li>
        <li class="nav-item">
          <%= link_to "English", new_coin_path(locale: "en"), class:"nav-link" %>
        </li>
      </ul>
      <%= yield %> 
    </div>
</body>
<h1><%= t("new.coin") %></h1>

<%= render 'form', coin: @coin %>

<%= link_to t("links.back"), coins_path, class: "btn btn-primary" %>
