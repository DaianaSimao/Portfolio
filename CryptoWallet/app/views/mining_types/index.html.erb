  <body>
     <ul class="nav justify-content-end">
        <li class="nav-item">
          <%= link_to "Português", mining_types_path(locale: "pt-BR"), class:"nav-link"  %>
        </li>
        <li class="nav-item">
          <%= link_to "English", mining_types_path(locale: "en"), class:"nav-link" %>
        </li>
      </ul>
      <%= yield %> 
    </div>
  </body>

<p id="notice"><%= notice %></p>

<h1><%= t("menu.mining_type") %></h1>

<table class = "table table-hover">
  <thead>
    <tr>
      <th><%= t("table_description.description") %></th>
      <th><%= t("table_description.acronym") %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mining_types.each do |mining_type| %>
      <tr>
        <td><%= mining_type.description %></td>
        <td><%= mining_type.acronym %></td>
        <td><%= link_to t("links.show"), mining_type %></td>
        <td><%= link_to t("links.edit"), edit_mining_type_path(mining_type) %></td>
        <td><%= link_to t("links.delete"), mining_type, method: :delete, data: { confirm: t("messages.confirm") } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t("new.mining_type"), new_mining_type_path, class: "btn btn-primary" %> |
<%= link_to t("links.back"), welcome_index_path, class: "btn btn-primary"  %>
